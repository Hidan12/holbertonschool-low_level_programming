#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	char let;

	for(let = 'a' ; let <= 'z'; let++)
	{
		if ()
	}
		_putchar(let);
	_putchar('\n');
	return;
}

