#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main (void)
{

char st[] = "\"Programming is like building a multilingual puzzle";

puts(st);

return (0);
}
