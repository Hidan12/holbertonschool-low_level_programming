#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
(
 char sta = ("\"Programming is like building a multilingual puzzle");

puts(sta);

return (0);
}
